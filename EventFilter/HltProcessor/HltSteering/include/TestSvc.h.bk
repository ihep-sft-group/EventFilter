#include "SniperKernel/SvcBase.h"
#include <vector>
#include <map>
class TestSvc: public SvcBase
{
    public:
        TestSvc(const std::string& name);
        ~TestSvc();
        bool initialize();
        bool execute();
        bool finalize();
        int getCount(){return m_count;};
    private:
        int m_count;
        std::string                 m_string;
        std::vector<int>            m_vector_int;
        std::map<std::string, int>  m_str_int;
};
